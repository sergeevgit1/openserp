# Установка n8n-nodes-openserp

## Способ 1: Через интерфейс n8n (рекомендуется)

1. Откройте n8n
2. Перейдите в **Settings → Community Nodes**
3. Нажмите **"Install a community node"**
4. Введите: `n8n-nodes-openserp@1.0.9`
5. Нажмите **"Install"**
6. Перезапустите n8n, если потребуется

## Способ 2: Через командную строку

```bash
# В директории вашего n8n проекта
npm install n8n-nodes-openserp@1.0.9

# Или глобально
npm install -g n8n-nodes-openserp@1.0.9
```

## Способ 3: Через Docker

Добавьте в ваш Dockerfile:

```dockerfile
RUN npm install n8n-nodes-openserp@1.0.9
```

## Проверка установки

После установки узлы OpenSERP должны появиться в списке доступных узлов n8n.

## Версия 1.0.9

Версия 1.0.9 включает новую функциональность для работы с SSL-сертификатами:

### 🆕 Новые возможности:

- ✅ **Поддержка самоподписанных SSL-сертификатов** - новая опция "Allow Unauthorized Certificates"
- ✅ **Совместимость с корпоративными прокси** - возможность работы с HTTPS-серверами без валидных сертификатов
- ✅ **Гибкая настройка безопасности** - выбор между строгой проверкой SSL и отключенной

### 🔒 Настройка SSL

Для работы с серверами, использующими самоподписанные сертификаты:

1. В настройках узла OpenSERP найдите опцию **"Allow Unauthorized Certificates"**
2. Установите её в положение `true`
3. Это отключит проверку SSL-сертификата при подключении

⚠️ **Внимание:** Используйте эту опцию с осторожностью и только для доверенных сетей.

### Исправления из предыдущих версий:

- ✅ Исправлена проблема с путями к файлам credentials
- ✅ Добавлена правильная структура директории dist
- ✅ Обновлены все необходимые зависимости
- ✅ Добавлена операция "Health Check" для проверки сервиса
- ✅ Улучшена совместимость с последними версиями n8n

## Требования

- n8n версии 0.200.0 или выше
- Node.js версии 16 или выше
- Доступ к API OpenSERP (если требуется)

## Устранение неполадок

Если узел не появляется после установки:

1. Перезапустите n8n
2. Проверьте консоль на наличие ошибок
3. Убедитесь, что версия n8n соответствует требованиям
4. Попробуйте переустановить пакет:

```bash
npm uninstall n8n-nodes-openserp
npm install n8n-nodes-openserp@1.0.9
```

### Проблемы с SSL

При возникновении ошибок SSL-соединения:

1. Убедитесь, что ваш сервер использует валидный SSL-сертификат
2. Если используется самоподписанный сертификат, включите опцию "Allow Unauthorized Certificates"
3. Проверьте настройки прокси и файрвола

## Поддержка

При возникновении проблем:
1. Проверьте [README](./README.md) для получения дополнительной информации
2. Посмотрите [примеры использования](./USAGE_EXAMPLES.md)
3. Создайте issue на [GitHub](https://github.com/sergeevgit1/openserp/issues)

## Безопасность

- Используйте валидные SSL-сертификаты в производственных средах
- Включайте опцию "Allow Unauthorized Certificates" только для доверенных сетей
- Регулярно обновляйте пакет до последней версии